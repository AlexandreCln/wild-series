<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="/">
            <img src="/images/clapperboard.svg" width="70" height="50" class="d-inline-block align-center logo" alt="logo Wild Series">
            Wild Series
        </a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('program_index') }}">Séries</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('category_index') }}">Catégories</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('actor_index') }}">Acteurs</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Mes Favoris</a>
                </li>
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnection</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_login') }}"> Connexion</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if app.user %}
                        <p class="align-self-center" style="color: lightgray;">Bienvenue {{ app.user.username }} !</p>
                    {% endif %}
                </li>

            </ul>
        </div>
    </nav>
</header>
