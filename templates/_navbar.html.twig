<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="/">
            <img src="/images/clapperboard.svg" width="70" height="50" class="d-inline-block align-center logo"
                 alt="logo Wild Series">
            Wild Series
        </a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('wild_index') }}">Séries</a>
                </li>
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('program_index') }}">Admin Séries</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('category_index') }}">Admin Catégories</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('actor_index') }}">Admin Acteurs</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="#">Mes Favoris</a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnection</a>
                {% else %}
                    <a class="nav-link" href="{{ path('app_login') }}"> Connexion</a>
                    </li>
                {% endif %}
                {% if app.user %}
                    <li class="nav-item">
                        <p class="align-self-center" style="color: lightgray;">Bienvenue {{ app.user.username }} !</p>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
</header>
