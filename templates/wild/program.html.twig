{% extends 'base.html.twig' %}

{% block title %}{{ programName }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="media-body">
            <h1 class="mt-0">Saisons de {{ programName }} :</h1>

            {% for season in seasons %}
                <a href="/wild/season/{{ season.id }}"><h2><span
                                class="badge badge-secondary">{{ season.description }}</span> ({{ season.year }})</h2>
                </a>
            {% endfor %}
        </div>

        <a href="{{ path('wild_index') }}">
            <button class="btn btn-primary">Retour aux séries</button>
        </a>
    </div>
{% endblock %}
